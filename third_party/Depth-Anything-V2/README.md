Bundled inference code from [Depth Anything V2](https://github.com/DepthAnything/Depth-Anything-V2).

Only the `depth_anything_v2` Python package is included here. Download the ViT-S checkpoint separately and set `model.depth_anything_pretrained` in the config (default: `checkpoints/depth_anything_v2_vits.pth`).
